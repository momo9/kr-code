#ifndef STACK_API_H
#define STACK_API_H

// get the top in stack, but do not pop it
double get_top();

// push a element the same as top
void cpy_top();

// swap top and the element under top
void swap_top();

#endif
